language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t mgregoire/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true mgregoire/docker-react npm run test